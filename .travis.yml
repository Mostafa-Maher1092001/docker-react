sudo:required
services:
  -docker


before_install:
  -docker build -t  Mostafa-Maher1092001 /docker-react -f Dockerfile.dev . 

 script:
   -docker run -e CI=true      Mostafa-Maher1092001 /docker-react npm run test 
